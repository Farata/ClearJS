Ext.define('**{app.name}**.controller.ApplicationController', {
    extend: 'Ext.app.Controller'
/*   
  	,
    stores: ['my.custom.Store'],
    refs: [{
       ref: 'myPanel',
       selector: 'mylist'   // panel defined with alias: 'widget.mylist',
    }],
    
    init: function() {    	
        this.control({
            'mylist button[action=delete]': {
                click: this.deleteSelectedCompanies
            }
        });
    },

    deleteSelectedCompanies: function() {
        var store = this.getMyCustomStore(),
            panel = this.getMyPanel(),
            view = panel.getView(),
            selectionModel = view.getSelectionModel(),
            selectedRecords = selectionModel.getSelection();

        store.remove(selectedRecords);
    }
*/    
});
